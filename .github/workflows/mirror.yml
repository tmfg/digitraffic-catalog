name: "Mirror repo to public"
on:
  push:
    branches:
      - master
  workflow_dispatch:
jobs:
  Mirror-action:
    if: github.repository != 'tmfg/digitraffic-catalog'
    runs-on: ubuntu-26.04
    steps:
      - name: Mirror
        uses: tmfg/digitraffic-actions@80082556de86d5f8643e40312ae9f4cdfcbd4b82 # mirror/v3
        with:
          mirror-repo: git@github.com:tmfg/digitraffic-catalog.git
          ssh-private-key: ${{ secrets.SSH_MIRROR_KEY }}
          verbose: 'true'
